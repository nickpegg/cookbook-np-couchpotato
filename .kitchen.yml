---
driver:
  name: vagrant
  customize:
    memory: 1024

provisioner:
  name: chef_zero

platforms:
  - name: debian-8.2
  - name: ubuntu-14.04

suites:
  - name: default
    run_list:
      - recipe[np-couchpotato::default]
    attributes:
